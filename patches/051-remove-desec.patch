PATCH 051 — remove deSEC from the wizard (the suite has no deSEC consumer; the DNS story is the
operator's own — the domain record is created in the pilot's migration, not registered through AIO).

THIS FILE IS THE SPEC the patch-queue entry is cut from. The queue's replay infrastructure is
the installer program's scope (its S4); until it exists, this manifest is the contract, and the
patch is cut against the THEN-current upstream tree, not blindly against the anchors below.
Anchors were verified at design time against the mirror @ 55024222 and RE-VERIFIED AT IMPLEMENT
(2026-09-21) against the fork at the same commit: every anchor holds, with the two corrections
recorded inline.

DELETION MANIFEST (every anchor verified against the mirror @ 55024222):

  php/templates/containers.twig
    :123-139  DELETE the "Don't have a domain? Get a free one from deSEC" details block —
              entry cards, modal markup, and the script that drove the registration window
    :145      REWORD the hints paragraph — drop the deSEC sentence, keep the port-forwarding
              guidance (it applies to any domain)
    :355-356  DELETE the `{% if is_desec_domain %}` include of desec-domain-notice.twig
    :405-419  DELETE the "deSEC account credentials" section (h2 + the stored-password reveal)

  php/templates/includes/community-containers.twig        [CORRECTED AT IMPLEMENT: the file
    :4-5      DELETE the `{% if is_desec_domain %}` include of desec-domain-notice.twig —     lives under
              the community-containers intro                                              templates/includes/,
                                                                                             not templates/]

  WHOLE FILES, DELETED
    php/templates/desec.twig
    php/templates/includes/desec-domain-notice.twig
    php/templates/includes/desec-register.twig
    php/public/desec-modal.js
    php/public/desec-done.js

  php/public/style.css
    :499      DELETE the "deSEC registration modal (containers page)" comment + its .modal block
    :550-566  DELETE the "deSEC modal view body (desec.twig)" comment + .desec-modal-body,
              .desec-modal-done rules

  TESTS — the three specs and their invocations go; the mock infra STAYS
    DELETE  php/tests/tests/desec-register.spec.js
    DELETE  php/tests/tests/desec-existing.spec.js
    DELETE  php/tests/tests/desec-existing-slug.spec.js
    DELETE  php/tests/run.sh:74-79 — the three default-suite `run_tests tests/desec-*.spec.js`
            invocations and their `sleep 1` separators (measured: :74-79; the research's :60-64
            was approximate)
    DELETE  .github/workflows/playwright-on-push.yml — the three "Run Playwright tests for desec …"
            steps (measured: :44/:47/:50, following the restore-instance step at :39-40)
    DELETE  .github/workflows/desec-api-version-watch.yml — the whole file
    STAYS   php/tests/tests/helpers.js — NOT desec-only: initial-setup.spec.js and
            restore-instance.spec.js import logInToContainersPage from it
    STAYS   php/tests/desec-mock.mjs + run.sh:38's desec-mock
            container loop entry — inert once no spec starts the mock; kept per this design's
            own decision (minimal-deletion: only operator-visible surface and spec invocations
            go), and noted inert in the patch preamble.
            [CORRECTED AT IMPLEMENT: there is no separate seed-desec-mock-config.php at this
            tree — the mock's seeding is desec-mock.mjs's own /__control/seed-domain endpoint
            (desec-mock.mjs:88), which stays with the file]

  PHP — STAYS, untouched (the zero-PHP rule: the patch queue never touches PHP)
    php/src/Controller/DesecController.php, php/src/Desec/DesecManager.php,
    php/src/Cron/UpdateDesecIp.php (cron.sh's no-op call remains a no-op), and every desec key
    in ConfigurationManager. Consequence, accepted in the design: `GET /desec` renders a
    template that no longer exists — a 500 if ever visited. Acceptable because the UI that
    reached it is gone; unreachable code with unreachable callers is dead, and deleting PHP is
    a different patch class (and FRD scope) entirely.

ORDERING — THE LOAD-BEARING CONSTRAINT:
  051 lands in the 051-059 gap, BEFORE 060. 060's es-CL hunks are cut against the DE-SEC'd tree;
  landing 051 after 060 would break 060's replay on every upstream release. The patch queue's
  apply order is numeric — this constraint is why the number 051 is not cosmetic.